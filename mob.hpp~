#pragma once
#include <iostream>

class Mobs
{
	friend class Fight;
	friend class Items;
  friend std::ostream& operator<<(ostream& out, Mobs s);

	private:

		int attack;
		int deff;
		int MP;
		int HP;


	public:

		void Skeleton();
		void Lurker();
		void Hydralisk();
		void Zealot();
		void HighTemplar();
		void Archon();
};
